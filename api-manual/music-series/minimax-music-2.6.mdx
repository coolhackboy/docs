---
title: "MiniMax Music 2.6"
description: "Generate complete music tracks from a style prompt, lyrics, or instrumental instructions"
icon: "music"
openapi: "/api-manual/music-series/minimax-music-2.6.json POST /api/generate/submit"
---

<Tip>
1. After submission, a `task_id` will be returned. If you provided a `callback_url`, when the task status becomes `finished` or `failed`, a POST request will be sent to the `callback_url`.
2. Regardless of whether `callback_url` is provided, you can retrieve the response result through the unified [Query Music Detail](/api-manual/music-series/query-music-detail) endpoint.
</Tip>

## Usage Guide

- This endpoint generates complete tracks with singing, backing music, and detailed arrangements from a style prompt
- You can provide custom lyrics, let the model optimize lyrics from the prompt, or enable instrumental mode for music without vocals
- Final results are returned asynchronously through `callback_url` or [Query Music Detail](/api-manual/music-series/query-music-detail)

## Parameter Details

- `prompt` (required): Music prompt describing style, mood, genre, instrumentation, tempo, or scene
  - Length: `10-2000` characters
  - Be specific about the musical direction you want in the final track

- `lyrics`: Optional lyrics content for vocal generation
  - Required unless `is_instrumental` or `lyrics_optimizer` is `true`
  - Maximum length: `3500` characters

- `lyrics_optimizer`: Optional boolean
  - When `true` and `lyrics` is empty, the model auto-generates lyrics from the `prompt`
  - Use this when you want a vocal song but do not want to provide exact lyrics

- `is_instrumental`: Optional boolean
  - Set to `true` to generate instrumental-only music without vocals
  - When `true`, omit `lyrics` and do not enable `lyrics_optimizer`

- `audio_setting`: Optional output audio settings object
  - `sample_rate`: `16000`, `24000`, `32000`, `44100`
  - `bitrate`: `32000`, `64000`, `128000`, `256000`
  - `format`: `mp3`, `wav`, `pcm`
  - Only these three keys are supported

## Developer Notes

- If both `is_instrumental` and `lyrics_optimizer` are `false`, provide `lyrics` to avoid validation errors
- `lyrics_optimizer` follows the upstream MiniMax Music 2.6 schema. If the upstream service rejects an optimized-lyrics request, retry with explicit `lyrics`
- Query results for this model return `audio_id`, `audio_url`, `title`, `duration`, and `prompt`
